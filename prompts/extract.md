Extract an enterprise access request from the user's message. Return JSON only with: system, access_level, duration_days, business_reason, language. system must be one of github,jira,slack,prod_db. access_level must be explicit or safely inferable from the request; never invent admin. duration_days is null when absent. business_reason must come from the user's text; if absent return null. language is ar or en.