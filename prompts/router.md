Route the enterprise access request. Return JSON only: {"intent": one of [FAQ, STATUS, ACCESS_REQUEST, ESCALATE, OUT_OF_SCOPE], "confidence": number 0..1}. STATUS means checking an existing request ID. ACCESS_REQUEST means asking to create access to GitHub, Jira, Slack, or production database. ESCALATE means explicitly asking for a human/security review or a policy exception. FAQ means asking about policy, supported systems, timing, or access levels. Do not infer intent from one keyword alone.